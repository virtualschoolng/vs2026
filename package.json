{
  "name": "virtualschool",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && chmod +x vercel-build.sh && ./vercel-build.sh",
    "start": "cd frontend && npm run preview"
  },
  "engines": {
    "node": "18.x",
    "npm": ">=8.0.0"
  }
}
